[pytest]
markers =
    statistical_slow: slow statistical validity tests gated for nightly runs
    integration_slow: full end-to-end fixture integration scenarios
    perf: performance benchmark and regression checks
    matlab_engine_optional: optional MATLAB engine parity tests
